<% if current_user.like?(post) %>
  <td><%= link_to 'UnLike', post_like_path(post, current_user.likes.find_by(post_id: post.id)), method: :delete %></td>
<% else %>
  <td><%= link_to 'Like', post_likes_path(post_id: post.id), method: :post %></td>
<% end %>